# 🛠️ API Development Skill

When working in `app/api/`:
1. **Multitenancy**: Ensure every endpoint validates the session and filters data by `company_id`.
2. **Error Handling**: Use standard HTTP status codes. Return clean JSON errors.
3. **Validation**: Use Zod or similar for input validation.
4. **Performance**: Use Edge Runtime where possible for webhooks.
